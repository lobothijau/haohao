# Nihao implementation update

The original plan has been finished but there are many things that still not working as expected:

1. We need to show the list of stories on homepage or landing page directly instead of asking using to login first. 
2. Throw away boilerplate code for the dashboard coming from Laravel strater kit we won't use that. 
3. We're going to implement a mobile first web app, the desktop too are just a mobile view with tablet view port for now. 
4. SRS feature is not implemented yet. 
5. Currently we're working with one sentence mode, if I inspect duchinese, they actually have the whole chinese story in one go. And I think they process it on the fly inside some kind of canvas. 
6. Now when we seelct any segemented words, we're opening a sidebar, that too far away show a popup dict instead. 

